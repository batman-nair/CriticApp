SECRET_KEY=<random big value>
OMDB_API_KEY=<Get API key from OMDb>
RAWG_API_KEY=<Get API key from RAWG>

# Database configuration
DB_NAME=criticdb
DB_USER=criticuser
DB_PASSWORD=<strong_password>
DB_HOST=db
DB_PORT=5432

# Production deployment settings
# Comma-separated domain names (e.g., example.com,www.example.com)
ALLOWED_HOSTS=localhost,127.0.0.1

# Comma-separated URLs with scheme (e.g., https://example.com,https://www.example.com)
CSRF_TRUSTED_ORIGINS=https://example.com

# Security settings (set to True in production with HTTPS)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
